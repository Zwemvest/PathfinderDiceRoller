---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - .github/workflows/deploy.yml
autonomous: false
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03

must_haves:
  truths:
    - "GitHub Actions workflow exists that builds and deploys on push to main"
    - "The app is accessible at zwemvest.github.io/PathfinderDiceRoller after deployment"
    - "The deployed app is installable as a PWA from a mobile browser"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "CI/CD pipeline: build on push to main, deploy to GitHub Pages"
      contains: "deploy-pages"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "build/"
      via: "npm run build outputs to build/, workflow uploads build/ as pages artifact"
      pattern: "upload-pages-artifact"

user_setup:
  - service: github-pages
    why: "GitHub Pages must be enabled for the repository to serve the deployed site"
    dashboard_config:
      - task: "Enable GitHub Pages with GitHub Actions source"
        location: "GitHub repo > Settings > Pages > Source > Select 'GitHub Actions'"
---

<objective>
Create the GitHub Actions CI/CD workflow for automatic deployment to GitHub Pages, then verify the full deployment pipeline works end-to-end including PWA installability.

Purpose: This completes the deployment story — every push to main automatically builds and deploys. The checkpoint confirms the live site works, is installable, and functions offline.

Output: Working CI/CD pipeline and verified live deployment at zwemvest.github.io/PathfinderDiceRoller.
</objective>

<execution_context>
@C:/Users/XPRTZRikOostveen/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/XPRTZRikOostveen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions deployment workflow</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
Create `.github/workflows/deploy.yml` with the following configuration:

- Trigger: push to main branch
- Permissions: contents read, pages write, id-token write
- Concurrency group: pages (cancel-in-progress)
- Build job: checkout, setup-node 20 with npm cache, npm ci, npm run build with NODE_ENV=production, upload-pages-artifact from build/
- Deploy job: needs build, github-pages environment, deploy-pages action

Use actions/checkout@v4, actions/setup-node@v4, actions/upload-pages-artifact@v3, actions/deploy-pages@v4.

Key points:
- NODE_ENV=production ensures paths.base is set to /PathfinderDiceRoller during build
- npm ci (not npm install) for reproducible builds
- build/ is the SvelteKit adapter-static output directory

After creating the file, verify it is readable: `node -e "require('fs').readFileSync('.github/workflows/deploy.yml', 'utf8')" && echo "YAML readable"`
  </action>
  <verify>
    <automated>cd /c/Users/XPRTZRikOostveen/Projects/PathfinderDiceRoller && test -f .github/workflows/deploy.yml && echo "Workflow file exists"</automated>
  </verify>
  <done>GitHub Actions workflow created at .github/workflows/deploy.yml. Workflow builds with production NODE_ENV and deploys build/ to GitHub Pages.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify deployment, PWA install, and offline functionality</name>
  <action>
This is a human verification checkpoint. Claude should push all Phase 1 code to main before this checkpoint. The user then verifies the deployment pipeline, PWA installability, and offline functionality.
  </action>
  <what-built>Complete Phase 1 app shell: SvelteKit PWA with bottom tabs, dice tray, IndexedDB storage, and GitHub Actions deployment pipeline. The workflow will trigger on the next push to main.</what-built>
  <how-to-verify>
1. Pre-deploy setup: Go to GitHub repo (github.com/zwemvest/PathfinderDiceRoller) then Settings then Pages then Source then select "GitHub Actions" (not "Deploy from a branch")

2. Push and deploy: After Claude commits and pushes all Phase 1 code, go to the GitHub repo Actions tab and verify the "Deploy to GitHub Pages" workflow runs successfully (green checkmark)

3. Visit the live site: Open https://zwemvest.github.io/PathfinderDiceRoller/ in Chrome on your phone
   - Verify: Three tabs visible at bottom (Character, Roll, History)
   - Verify: Dice tray bar visible above tabs
   - Verify: Tapping each tab navigates to a different page
   - Verify: No horizontal scrolling on mobile

4. PWA install test: In Chrome mobile, tap the menu (three dots) then "Add to Home Screen" or "Install app"
   - Verify: Install prompt appears with "Pathfinder Dice Roller" name
   - Verify: After install, the app opens in standalone mode (no browser chrome)

5. Offline test: With the PWA installed or the site loaded, enable airplane mode and reload
   - Verify: App still loads and displays the shell (tabs, dice tray)

6. Desktop check: Open the same URL on desktop
   - Verify: Layout is usable (no broken elements, tabs still work)
  </how-to-verify>
  <verify>Human confirms deployment, PWA install, and offline all work</verify>
  <done>Live deployment verified at zwemvest.github.io/PathfinderDiceRoller. PWA installable. Offline works.</done>
  <resume-signal>Type "approved" if the deploy, PWA install, and offline test all work. Otherwise describe what failed.</resume-signal>
</task>

</tasks>

<verification>
1. `.github/workflows/deploy.yml` exists with correct structure
2. GitHub Actions run completes successfully (green checkmark)
3. Live site loads at zwemvest.github.io/PathfinderDiceRoller
4. PWA is installable from browser
5. App works offline after initial load
</verification>

<success_criteria>
- GitHub Actions workflow triggers on push to main and deploys successfully
- Live site is accessible at the GitHub Pages URL
- App is installable as a PWA
- App works offline after initial load
- Layout is responsive on mobile and desktop
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
